import React from 'react';
import Layout from './layout';
import { Flex, Box } from 'theme-ui';

function Product({ product }) {
  return <h2>This is the product page</h2>;
}

export default Product;
